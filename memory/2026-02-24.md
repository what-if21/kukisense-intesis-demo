# 2026-02-24

## GitHub Repository Setup

Connected local workspace repositories to GitHub:

### Repositories
- **eink_frame_app** → https://github.com/what-if21/eink_frame_app
- **eink-frame-firmware-idf** → https://github.com/what-if21/eink-frame-firmware-idf

### Setup Notes
- Both repos had existing content on GitHub with divergent histories
- Resolved by merging with `--allow-unrelated-histories` and keeping local changes
- Commits pushed: `2d6b7e0` (app), `f4992b5` (firmware)

### Main Workspace
- Main workspace folder (`~/.openclaw/workspace`) has git initialized but not yet linked to remote
- Contains: project docs, firmware, app code, PCB files, memory files
- 35+ untracked files ready for initial commit when remote is set up

## KukiSense TV Code Review & Updates

### Code Review Completed
- Reviewed Flutter TV app for IAQ monitoring
- Architecture: Provider-based state management, ThingsBoard integration
- Identified issues: error handling gaps, hardcoded values, missing connection state

### Improvements Implemented (Commit `9fb6552`)
1. **Connection State Indicator** (`lib/models/connection_state.dart`)
   - Added `ConnectionStatus` enum (connected, connecting, disconnected, error)
   - Visual indicator with pulsing dot for connecting state
   - Shows last successful fetch time when disconnected

2. **Centralized Config** (`lib/config/app_config.dart`)
   - Extracted all magic numbers (refresh intervals, thresholds, timeouts)
   - Configurable: `telemetryRefreshInterval`, `weatherRefreshInterval`, `historicalHours`

3. **Manual Refresh Button** (`lib/widgets/connection_indicator.dart`)
   - Tap-to-refresh when disconnected
   - Visual feedback via SnackBar confirmation
   - Resets error counter on manual refresh

### Files Modified
- `lib/providers/telemetry_provider.dart` - Added connection state tracking
- `lib/screens/dashboard_screen.dart` - Integrated connection indicator
- `lib/widgets/metric_card.dart` - Uses config threshold ratios
- `lib/widgets/connection_indicator.dart` - New widget
- `lib/models/connection_state.dart` - New enum
- `lib/config/app_config.dart` - New config file

Pushed to: https://github.com/what-if21/kukisense-tv

## ESP32-S3 2.8" Touch Display - Code Review & Updates

### Repository Created
- **Repo**: https://github.com/what-if21/esp32s3_2.8touch
- **Initial commit**: `55dab58` - ESP32-S3 display with LVGL, RGB LCD, touch, IMU

### Hardware
- ESP32-S3 (480x480 RGB LCD, ST7701S driver)
- GT911 capacitive touch
- QMI8658 IMU (accel + gyro)
- PCF85063 RTC, SD card, buzzer

### Code Review Findings
- Good architecture: System_Task + GUI_Task with semaphore
- Clean driver separation
- Missing: error handling, watchdog feeds, hardcoded UART config
- No power management

### Improvements Implemented (Commit `662f1a4`)

**1. IMU Error Handling (`QMI8658.c/h`)**
- All functions return `esp_err_t`
- Device detection with 3 retries
- WHO_AM_I validation (expect 0x05)
- I2C error logging with ESP_LOG
- Timeout handling for CTRL9 commands

**2. UART Kconfig Options**
- `CONFIG_SENSOR_UART_NUM` - UART port (0-2)
- `CONFIG_SENSOR_UART_BAUD_RATE` - baud rate
- `CONFIG_SENSOR_UART_TX/RX_PIN` - GPIO pins
- `CONFIG_SENSOR_UART_BUF_SIZE` - buffer size

**3. Power Management**
- Auto-backlight dimming (Kconfig)
- `CONFIG_ENABLE_AUTO_BACKLIGHT` - enable/disable
- `CONFIG_BACKLIGHT_DIM_TIMEOUT` - seconds of inactivity
- `CONFIG_BACKLIGHT_DIM_LEVEL` - dimmed brightness %
- Touch activity wakes display
- Timer checks every 10 seconds

**4. Watchdog Support**
- `esp_task_wdt_reset()` in QMI8658_Init and QMI8658_Loop
- Prevents WDT timeout during sensor operations

### Files Modified
- `main/QMI8658/QMI8658.c` - Error handling, watchdog
- `main/QMI8658/QMI8658.h` - Updated function signatures
- `main/LCD_Driver/ST7701S.c/h` - Auto-dimming functions
- `main/Touch_Driver/GT911.c` - Activity notification
- `main/LVGL_NEWUI/ui.c` - Use Kconfig UART settings
- `main/Kconfig.projbuild` - New configuration options
- `main/main.c` - Initialize auto-dimming

### UI Code Review & Fixes (Commit `b5f4b52`)

**Review Findings:**
- cJSON memory leak potential
- No bounds checking on sensor values
- Instant value changes (no animations)

**Fixes Implemented:**

**1. cJSON Memory Management**
- Add proper `cJSON_Delete(root)` after parsing
- Add JSON structure validation (check for `{` and `}`)
- Log warnings for invalid JSON frames
- Validate JSON root is an object before processing

**2. Sensor Value Bounds Checking**
- Add `sensor_min[]` and `sensor_max[]` arrays:
  - PM2.5: 0-999, Temp: -40 to 80°C
  - RH: 0-100%, CH2O: 0-10 mg/m³
  - CO2: 0-5000 ppm, TVOC: 0-5000 ppb
  - PM10: 0-999
- Add `clamp_sensor_value()` helper function
- Validate all values before display
- Support multiple JSON key variants (PM25/pm2.5/PM2.5, Temp/temperature/Temperature)
- Keep previous value if new value is out of range

**3. Smooth Animations**
- Add `lv_anim_t` structure for arc animation
- `animate_arc_to()` function for gauge transitions
- 300ms ease-out animation path
- Proper arc range setting (0-100) with bounds checking

### Files Modified
- `main/LVGL_NEWUI/ui.c` - Complete rewrite with fixes

Pushed to: https://github.com/what-if21/esp32s3_2.8touch

## Hazelnut IAQ Firmware (warrenedit branch)

### Repository Pulled
- **Repo**: https://github.com/NistantriTech/hazelnut_IAQ
- **Branch**: warrenedit
- **Commit**: d9db020 → ba39d3f (with CHANGELOG)
- **Firmware Version**: 0.0.22

### Hardware
- ESP32 with TFT LCD (128x160, SPI)
- M702 sensor (UART)
- SSD1306 OLED (I2C)
- Zigbee module (UART)
- LED strip (RMT)
- Speaker (MAX98357 I2S)
- Microphone

### Key Features (warrenedit branch)
- **BLE Local Mode**: Storage and export manually from app
- **Zigbee Integration**: Full stack with router/coordinator support
- **Offline Storage**: SPIFFS-based data persistence
- **OTA Support**: Both HTTP and Bluetooth OTA
- **Multiple Sensors**: M702, SFA30, SEN66 (I2C)
- **LED Status**: Advanced patterns and automation
- **WiFi Management**: Static IP, auto-reconnection
- **MQTT**: ThingsBoard integration with RPC

### CHANGELOG Created
- Documented 89 commits difference from master
- Categorized: Added, Fixed, Changed, Technical
- Branch comparison table
- Known issues and roadmap

### Files Reviewed
- `main/config.h` - Hardware configuration
- `main/tft_lcd_screen.c` - Display driver
- `main/zigbee_communication.c` - Zigbee stack (60KB)
- `main/mqtt_manager.c` - MQTT client (86KB)
- `main/bluetooth_manager.c` - BLE support
- `main/ota_manager.c` - OTA updates
- `main/spiffs_storage.c` - Offline storage
- `main/led_status_handle.c` - LED control

Pushed CHANGELOG to: https://github.com/NistantriTech/hazelnut_IAQ/tree/warrenedit

## Hazelnut Mobile App (KukiSense Controller)

### Repository Pulled
- **Repo**: https://github.com/what-if21/hazelnut.git
- **Local Path**: `/home/warren/.openclaw/workspace/hazelnut_app/`
- **Type**: Flutter mobile application

### App Structure
```
lib/
├── main.dart                 # App entry point
├── EspBleConfigure/          # ESP32 BLE configuration
├── constants/                # App constants
├── controller/               # Business logic controllers
├── firebase/                 # Firebase integration
├── oauth2/                   # ThingsBoard OAuth2 auth
│   ├── tb_oauth2_client.dart
│   ├── app_secret_provider.dart
│   └── web/
├── screens/                  # UI screens
├── storage/                  # Local storage
├── widget/                   # Reusable widgets
│   ├── adddevice_widget.dart
│   ├── device_one_column_card.dart
│   ├── device_two_column_card.dart
│   ├── color_picker_widget.dart
│   ├── qr_code_scanner.dart
│   └── ...
```

### Key Features
- **BLE Configuration**: Configure KukiSense devices via Bluetooth
- **ThingsBoard OAuth2**: Secure authentication with ThingsBoard
- **Device Management**: Add, configure, and control devices
- **QR Code Scanner**: Easy device pairing
- **Color Picker**: LED strip color control
- **Firebase**: Backend integration

### Purpose
Mobile app for:
1. Initial device setup and WiFi configuration
2. BLE-based local control when offline
3. Remote control via ThingsBoard
4. Device management and monitoring

### Integration Points
- Connects to `hazelnut_IAQ` firmware via BLE
- Communicates with ThingsBoard server
- Controls devices through MQTT/RPC

### Intesis AC Control Implementation

**Status:** Implemented and pushed to `warrenedit` branch

#### Flutter App Changes (hazelnut_app)
- **Branch:** `warrenedit`
- **Commit:** `0a67d1e` - Add Intesis AC Control widget and controller
- **Files Added:**
  - `lib/controller/ac_controller.dart` - AC control via ThingsBoard
  - `lib/widget/ac_control_widget.dart` - AC UI widget

#### Firmware Changes (hazelnut_IAQ)
- **Branch:** `warrenedit`
- **Commit:** `8cfc1d0` - Add Intesis AC Control support
- **Files Added:**
  - `main/intesis_ac/intesis_ac.c` - TCP/IP ASCII client
  - `main/intesis_ac/intesis_ac.h` - AC API definitions
- **Files Modified:**
  - `main/handle_rpc.c` - Add acControl RPC handler
  - `main/mqtt_manager.c` - AC telemetry integration
  - `main/CMakeLists.txt` - Include intesis_ac module

#### AC Features
- Power ON/OFF control
- Mode selection (COOL, HEAT, DRY, FAN, AUTO)
- Temperature setpoint (16-30°C, 0.5°C precision)
- Fan speed control (LOW, MEDIUM, HIGH, AUTO)
- Room temperature feedback
- Online/offline status
- Automatic status polling every 10s

#### Branch Structure
```
hazelnut_app:
├── master (598f959) - Base app without AC
├── feature/hazelnut_subscription (752871e) - BLE subscription features
│   └── Fixed Timebased Automation bug
│   └── Fixed Offline Automation bug
│   └── Storage and export manually from app for Ble mode
└── warrenedit (abe7302) - Based on feature/hazelnut_subscription + AC control
    └── AC Control page with navigation
    └── Quick actions (Comfort, Sleep, Eco)
    └── Configurable Intesis Gateway IP/Port
    └── ThingsBoard integration

hazelnut_IAQ:
├── master - Original firmware
└── warrenedit (f5a7d5a) - With AC support
    └── Intesis TCP/IP client
    └── acControl RPC handler
    └── AC telemetry integration
    └── Configurable gateway IP via shared attributes
```

#### Documentation
- `docs/INTESIS_AC_IMPLEMENTATION.md` - Full implementation guide
- `docs/THINGSBOARD_AC_WIDGET.md` - ThingsBoard widget setup

#### Implementation Complete
✅ Firmware TCP/IP client for Intesis gateway
✅ Firmware RPC handler (acControl method)
✅ Firmware telemetry integration
✅ Firmware configurable gateway IP via shared attributes
✅ Flutter AC controller (ThingsBoard attributes)
✅ Flutter AC widget (power, mode, temp, fan)
✅ Flutter AC page with quick actions
✅ Flutter configurable Intesis Gateway IP/Port
✅ Bottom navigation integration (5 tabs)
✅ ThingsBoard widget configuration
✅ ASCII protocol implementation

#### Architecture
```
Flutter App ←→ ThingsBoard ←→ hazelnut_IAQ ←TCP/IP→ Intesis Gateway ←→ AC Unit
```
